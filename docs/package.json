{
  "private": true,
  "devDependencies": {
    "@redocly/cli": "^1.19.0",
    "@asyncapi/cli": "^2.17.0"
  },
  "scripts": {
    "build:rest": "redocly build-docs ./openapi.yaml -o ./index.html",
    "build:ws": "asyncapi generate fromTemplate ./asyncapi.yaml @asyncapi/html-template -o ./asyncapi",
    "build": "npm run build:rest && npm run build:ws"
  }
}
