{
  "private": true,
  "devDependencies": {
    "redoc-cli": "^0.13.21",
    "@asyncapi/cli": "^2.17.0"
  },
  "scripts": {
    "build:rest": "redoc-cli build docs/openapi.yaml -o docs/index.html",
    "build:ws": "asyncapi generate fromTemplate docs/asyncapi.yaml @asyncapi/html-template -o docs/asyncapi",
    "build": "npm run build:rest && npm run build:ws"
  }
}
